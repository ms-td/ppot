# 分析モデル (話題沸騰ポット GOMA-1015型)

`docs/spec/` の要求仕様から作成した**分析モデル**を置く. 図は PlantUML で記述し, 同名の
PNG を隣に生成する.

## 分析モデルと設計モデルの位置づけ

| | 入力 | 読み手 | 目的 |
|---|---|---|---|
| **分析モデル** (このディレクトリ) | 要求仕様 (`docs/spec/`) | 人間 (仕様の理解・レビュー) | 要求仕様に何が書かれているかを図で説明する |
| **設計モデル** (今後 `docs/design/` を想定) | 分析モデル | 実装作業者 | 実装の指針を与える |

分析モデルは**要求仕様に書かれていること以上を決めない**. 実装上の判断 (クラス分割の粒度,
言語, データ構造, タスク分割など) は設計モデル側の仕事とする. そのため分析モデルの図は
**日本語のみ**で記述し, 要求仕様の用語をそのまま使う (英数字は要求IDと単位のみ).

## ファイル一覧

| ファイル | シーン |
|---|---|
| `object-01-デフォルト-水なし-蓋閉.puml` | コンセント接続直後のデフォルト値, 水なし, 蓋閉 → アイドルのまま |
| `object-02-給水線-沸騰開始.puml` | 給水線まで注水, 蓋閉, 3sec 経過 → 沸騰行為の開始あたり |
| `object-03-ミルク保温-ロック中.puml` | ミルクモードで保温中, 給湯口はロック中, 給湯で水位が減った状態 |
| `object-04-温度下がらずエラー.puml` | 図03の続き. ヒータが停止できなくなり `pot-500-21` を検知した直後 |

`2026-08-18_object-*.puml` は関連の線種の規約を確定する前の版で, 比較用に残してある.
`docs/analysis-analog/` にはプロジェクトオーナーが自力で描いた同一シーンの図があり,
畳み込み表記と共通クラスの型付けはそこから取り入れた.

今後 `class-NN-*.puml`, `state-NN-*.puml`, `sequence-NN-*.puml` を同じディレクトリに追加する.

### 4枚が受け持つ値の範囲

オブジェクト図はシーンごとの**スナップショット**なので, 4枚合わせてモデルの取りうる値を
できるだけ覆うように選んである. 4枚とも**同じオブジェクトを全部載せる**方針にしているので,
並べれば差分がそのまま読める.

| 属性 | 図01 | 図02 | 図03 | 図04 |
|---|---|---|---|---|
| 温度制御行為 | アイドル | 沸騰行為 | 保温行為 | エラー |
| 行為の段階 | ― | 加熱中 | 保温中 | ― |
| 保温モード | 高温 (未適用) | 高温 (未適用) | ミルク (適用中) | ミルク (停止) |
| 水量区分 | 空 | 適正 | 適正 | 適正 |
| 水位メータ | 全点滅 (空) | 4セル全点灯 | 2セル点灯 | 2セル点灯 |
| 操作量M | 0% | 100% | 14% (PID) | ― |
| ヒータ | off | on | 制御周期内で on/off | off |
| ヒータ用電源 | on | on | on | **off** |
| ブザー | off | off | off | **鳴動中 (30秒)** |
| ロック状態 / ロックランプ | 解除 / 消灯 | 解除 / 消灯 | **ロック中 / 点灯** | ロック中 / 点灯 |
| 沸騰ランプ / 保温ランプ | 消灯 / 消灯 | 点灯 / 消灯 | 消灯 / 点灯 | 消灯 / 消灯 (未定義) |
| 沸騰ボタン | 無効 | 無効 | **有効** | 無効 |
| 温度表示 | 非表示 | 23 | 60 | 99 (未定義) |

まだどの図でも現れていない値: **給湯中 / ポンプ作動**, **カルキ抜き中**, **満水 (水量異常)**,
**蓋開による温度制御中止**, **節約モード**. 追加シーンの候補は「要決着」の項を参照.

## 図の読み方

### package = 機能

package はハードウェアの分類ではなく**機能**で切る. 根拠は, 正典である sheet4 自身が
機能で構成されているからである (B列の大見出し):

```
B2:  ＜構成要素と動作モード＞   pot-210 コンセント / pot-220,221 蓋 / pot-230 沸騰ボタン / pot-240 保温モード
B59: ＜操作要求仕様＞         pot-250 解除ボタン / pot-260 給湯ボタン / pot-280 水位メータ
B100:＜温度制御行為＞         pot-310 沸騰行為 / pot-320 保温行為 / pot-330 アイドル
B157:＜温度制御方式＞         pot-400
B186:＜エラー検知＞           pot-500
```

sheet3 の「外観 / 内部構成 / 操作パネル部」はハードウェアの分類にすぎず, 分析モデルが従うべき
構造ではない. 6つの package は次のとおり.

| # | package | 対応する要求 | 主なオブジェクト |
|---|---|---|---|
| 1 | ポット全体と電源 | `pot-210`, `pot-100-12`, `pot-100-15`, `pot-110-22` | ポット, 貯水部, 操作パネル, ブザー |
| 2 | 蓋の開閉 | `pot-220`, `pot-221`, `pot-100-11`, `pot-110-15` | 蓋, 蓋センサ |
| 3 | 水位の判定と表示 | `pot-280`, `pot-110-11`/`-13`/`-19`, `pot-120-11` | 満水センサ, 第1〜第4水位センサ, 給水線, 水位判定, 水位メータ, インジケータセル×4 |
| 4 | 温度制御 | `pot-230`, `pot-240`, `pot-310`〜`330`, `pot-400`, `pot-110-16`/`-17` | サーミスタ, ヒータ, 温度制御行為 (アイドル/沸騰行為/保温行為), 保温モード設定, 沸騰ボタン, 保温設定ボタン, 沸騰ランプ, 保温ランプ, 温度/モード表示窓 |
| 5 | ロックと給湯 | `pot-250`, `pot-260`, `pot-100-13`, `pot-110-20` | 給湯口, 解除ボタン, 給湯ボタン, ロック状態, ロックランプ, 給湯状態, ポンプ |
| 6 | エラーの検出と処置 | `pot-500`, `pot-110-18` | エラー検出, エラー, ヒータ用電源 |

**エラーオブジェクトは 4.温度制御 ではなく 6.エラー に置く.** 状態遷移シート 6.1/6.2 で
エラーは沸騰行為・保温行為と並ぶトップレベル状態かつトラップ状態として描かれているためで,
図04 では「現在の状態」が package 4 から package 6 へ移動する形になる.

### 複数の機能から使われるオブジェクトの所属規則

PlantUML の package は重複できない. 一方, 複数の機能から使われるオブジェクトが存在する.

| オブジェクト | 使う機能 |
|---|---|
| 蓋センサ `pot-110-15` | 蓋判定 `pot-220-11`/`pot-221-11`, 水位判定のタイミング `pot-280-11`/`-12`, モード切替条件 `pot-240-21`, ロック操作条件 `pot-250-21`/`-31`, 給湯条件 `pot-260-11`, 温度制御停止 `pot-310-31`/`pot-320-31` |
| 第n水位センサ `pot-110-13` | 水位判定 `pot-280`, 温度制御停止 `pot-310-31`/`pot-320-31`, 給湯条件 `pot-260-11` |
| サーミスタ `pot-110-16` | 温度制御 `pot-400`, カルキ抜き `pot-311-11`, エラー検出 `pot-500-*` |
| ヒータ用電源 `pot-110-18` | 給湯条件 `pot-260-11`/`-21`, エラー処置 `pot-500-*` |
| ブザー `pot-110-22` | ボタン受付 `pot-230-11`/`pot-240-21`, エラー `pot-500-*` |

**規則: その値を決める (書き込む) 機能が持ち, 読むだけの機能は破線で参照する.**

- 蓋センサ → 値は物理の蓋が決めるので **2.蓋の開閉**. 3/4/5 が条件として参照する
- 満水センサ・第n水位センサ → **3.水位**. 4/5 が参照する
- サーミスタ → **4.温度制御**. 6 が参照する
- ヒータ用電源 → off にするのはエラー処置だけなので **6.エラー**. 4/5 が参照する
- ブザー → 3つの機能から鳴らされ単独のオーナーがいないので **1.ポット全体**

副次的な効果として, **蓋センサが多数の機能から参照されるハブであること**が図から読み取れる.
これはこの系で最も影響範囲の広いセンサなので, 分析結果として意味がある.

失うものもある. 物理的な取り付け関係 (貯水部にセンサが付く, 蓋にパネルがはまる) が package
では表せなくなるため, 貯水部と操作パネルには `取り付け部品` / `配置部品` という属性を持たせて
補っている.

### 色 = 種別

package が機能を表すので, オブジェクトの**種別は色**で表す.

| 色 | 種別 |
|---|---|
| 橙 `#FFF0DB` | センサ (入力ハードウェア) |
| 赤 `#FFDFDF` | アクチュエータ (出力ハードウェア) |
| 緑 `#E2F3E2` | 操作パネルの部品 (ボタン・ランプ・表示窓・水位メータ) |
| 青 `#D6E4FF` | 状態・判定 |
| 桃 `#FFD9D9` | エラー (図04のみ) |
| 白 | 外観の構造物 |

### 属性値の根拠注記

すべての属性値の末尾に**根拠**を付ける. これが「人間向け説明図」の核になる.

| 記法 | 意味 |
|---|---|
| `【pot-280-41】` | 要求仕様でその値が決まっている. IDは `docs/spec/要求仕様.txt` / `ハードウエア構成とハードウエア要求仕様.txt` を grep すれば引ける |
| `【シーン仮定】` | 仕様では決まっておらず, このシーンを描くために置いた値 (水温の実測値, ボタンの押下状態など) |
| `【仕様未定義】` | 仕様書に記述がない項目 (制御周期, Kp/Ki/Kd, ブザー周波数, エラー時の表示) |

推論が混ざる場合は併記する
(例: `検出値 = off 【シーン仮定: 給水線まで注水】【pot-110-19: 給水線は満水センサより若干下】`).

各図では**直前の図から変化した値を `<b>太字</b>`** にしている.

### 関連の判定手順 (線の意味)

**オブジェクト図でオブジェクト間の実体関係として通常表す線はリンク, つまり関連 (association) の
インスタンスであり, 実線1種類である.** 「破線のリンク」というものはない. 依存 (dependency, 破線)
は「供給側の**定義**が変わると利用側の定義が影響を受ける」という定義レベルの結合を指すので,
「実行時にセンサの値を読む」に使うのは依存の意味の流用である.

なお **合成 ◆── もリンクである**. 合成はクラス図における関連端の性質であり, オブジェクト図で
描いているのは「合成関連に基づくリンク」である. 本 README では簡便のため単に「合成」と呼ぶ.

そこで関連を3種に分け, 次の手順で判定する.

```
Q1. 仕様が「AはBを部位・構成要素として持つ」と明示していて,
    この図で全体-部分として読ませる必要があるか
      → はい: 合成 ◆──  (UML標準)
Q2. 仕様がAとBの対応を明示していて, 多重度が言えるか
    = 「モデルはその対応を覚えていなければならないか」
      → はい: リンク ───  (UML標準)
Q3. AはBの値をその場で読むだけか
      → はい: 線を引かない
```

**寿命感覚 (片方が消滅したらもう片方も消滅する) は合成の意味そのもの**であり, それが成り立つか
どうかが合成の**意味的な妥当性**を決める. ただし妥当だからといって描くべきとは限らない.
この系では sheet3 ＜＜内部構成＞＞ がセンサ・ヒータ・ポンプ・ブザーなどをすべてポットの内部部品
として列挙しているため, 寿命だけを文字通り適用すると内部部品9個がすべて `ポット` からの合成に
なり, 長い線が9本増えて可読性を損なう. そこで Q1 は「**この図で全体-部分として読ませる必要が
あるか**」まで含めた問いにしてある.

そして**寿命感覚だけでは Q2 と Q3 が決まらない**. 物理部品を含む系では Q3 が多数になるため,
寿命だけを尺度にすると参照が爆発する. Q2 の「モデルは覚えているか」を主たる尺度にする.

判定結果:

| 対 | 種類 | 根拠 |
|---|---|---|
| ポット → 貯水部 / 蓋 / 給湯口 | 合成 | sheet3 ＜＜外観＞＞ がポットの部位として列挙 |
| 蓋 → 操作パネル | 合成 | `pot-100-12`「ポットの蓋にはめ込まれている」 |
| 操作パネル → 各ボタン / 各ランプ / 表示窓 / 水位メータ | 合成 | sheet3 操作パネル表の「部位」列, `image13.png` |
| 水位メータ → インジケータセル | 合成 `1..*` | `pot-120-11`「水位センサと同数のインジケータセル…により構成される」 |
| **蓋 — 蓋センサ** | **リンク** | `pot-110-15` は「蓋が閉じているかどうかを検出する」としか言わず, センサが蓋側に付くとは書いていない. `image12.png` でも本体側に描かれている |
| 貯水部 — 満水センサ / 第n水位センサ / 給水線 | リンク | 位置に意味がある (`pot-110-19`「満水センサの位置よりも若干下」) |
| **貯水部 — サーミスタ / ヒータ** | **リンク** | `pot-110-16`「ポット内の水温を検出」, `pot-110-17`「ポット内の水を加熱」. 貯水部の部品とは書いていないので作用対象として結ぶ |
| インジケータセル — 第n水位センサ | リンク `{1対1×4}` | `pot-280-21`「onになっている第nセンサに**対応した**インジケータセル」. 物理的包含はないが仕様が対応を要求 |
| ヒータ用電源 — ヒータ | リンク | `pot-110-18`「ヒータへ電力を供給する」. 同時に「電力供給状態≠ヒータ動作中」と分離を明示 |
| 給湯口 — ポンプ | リンク | `pot-110-20`「吸い上げて、給湯口から排出する」 |
| ポット — 現在状態 | リンク `[1]` | 状態遷移シート 6.1. 常にちょうど1つの状態にある |
| 水位判定 → 各センサ / 蓋センサ | 線にしない | `pot-280-11`/`-12` は「蓋センサonから3sec経過した時点で」検出する. 永続リンクではない |
| 温度制御 → サーミスタ | 線にしない | 値を読むだけ |
| 給湯状態 → 6条件 (`pot-260-11`) | 線にしない | 複合ガード. 6本の線にすると図が壊れる |
| 温度制御 → 停止条件 (`pot-310-31`/`pot-320-31`) | 線にしない | 状態図のガードが本籍 |
| **ブザー** | **構造線を引かない** | sheet3 ＜＜内部構成＞＞ は `pot-110-22` を列挙するが所属先を書かない. 他の内部部品は作用対象へのリンクで位置づくが, ブザーには作用対象がなくポット全体への報知装置である. 内部部品を全部 `ポット` からの合成にすると線が9本増えるため, package 1「ポット全体」への配置で所属を示す |

### 説明線 — 最小限の逸脱 (UML標準ではない)

分析モデルは最終的に人向けの説明に使うので, 規約を厳格に守った結果で説明がしんどくなるのは
本末転倒である. そこで**各図4本だけ**, 非標準の破線を「説明線」として許す.

| 説明線 | なぜ落とせないか |
|---|---|
| `現在状態 ..> ヒータ` `<<説明線:制御出力>>` | `pot-400-11`「ヒータを操作量の%数だけonし、それ以外はoffする」が温度制御方式の核心. 操作量Mは状態オブジェクトが持ち, ヒータの動作状態はそこから導かれるので, 属性だけでは因果が読めない |
| `水位判定 ..> 水位メータ` `<<説明線:表示反映>>` | `pot-280` は判定要求であると同時に表示要求. 判定結果が表示へ行くことが人向け説明で重要 |
| `エラー検出 ..> ヒータ用電源` `<<説明線:処置>>` | `pot-500-21` が「ヒータ用電源をoffして」と処置を直接規定. 図04の主題そのもの |
| `エラー検出 ..> ブザー` `<<説明線:処置>>` | 同上「30秒間ブザーを鳴らす」 |

図04では `現在状態 ..> ヒータ` の代わりに
`エラー検出 ..> 現在状態` `<<説明線:状態遷移>>` を使う (温度制御行為が存在しないため).

状態 note の制約には**満水センサ on も温度制御停止条件として書いている** (「決着済みの判断 1」).
sheet4 の `pot-310-31`/`pot-320-31`/`pot-330-12`/`-13` は literal には蓋センサ off と
全水位センサ off の2つだけを挙げるため, 制約の中では `∨ 満水センサon (決着事項)` と注記して
仕様原文との差を明示している.

各説明線のラベルは **`<<説明線:用途>>` で始める**. 凡例だけでなく線ごとに非標準であることが
分かるようにするため.

各ランプ・表示窓への出力は説明線にしていない. `pot-310-11` などの引用で辿れるので線は冗長で,
「最小限の逸脱」に反するため.

### 「線を引かない」情報の行き先

| 行き先 | 何を書くか |
|---|---|
| 属性値の根拠注記 | このシーンで確定している値と, その直接の根拠ID |
| note + 制約 `{…}` | 複合条件. `pot-260-11` の給湯6条件, `pot-330` のアイドル出入り条件, `pot-280-11`/`-12` の水量判定条件. **note と `{…}` の制約記法は UML 標準なので逸脱にならない**. ただしここで書いている `{1対1×4}` や日本語の論理式は**自然言語の制約**であって OCL 等の形式制約ではない |
| シーケンス図 (今後) | 時間条件とイベント列. 蓋センサon 3sec後の水位判定, 100msec押下→ブザー→遷移, 1分周期の水温検出→エラー処置 |
| クラス図 (今後) | 多重度と安定した対応. 全体の構成木もここで示す |

### 畳み込み表記 (UML標準ではない)

`第1〜第4水位センサ [4実体]` と `インジケータセル1〜4 [4実体]` は, 同型で使い方もほぼ同じ
4実体を1枠に畳み, 区画ごとに別インスタンスを表す非標準表記 (UML 1.x の multiobject に近い).
根拠は `pot-280` の説明「水位センサはコストダウンによって数が変動する可能性があるため,
一個一個に意味を持たせないことで仕様変更に対応しやすく考える」で, 畳んでも情報を失わない.
実体ごとに値が違う場合 (図03・図04) も区画ごとに書き分けられる.

対応関係は同一の線を4本描くのではなく, リンク1本に制約 `{1対1×4}` を付けて表す.

## モデル語彙 (概念クラス一覧)

後続の図と設計モデルはこの語彙を変えずに使う. 表は package 順.

### 1. ポット全体と電源

| 概念クラス | ID | 主な属性 |
|---|---|---|
| ポット | ― | `温度制御行為` |
| 貯水部 | `pot-100-15` | `水量` / `取り付け部品` |
| 操作パネル | `pot-100-12` | `取付先` / `配置部品` |
| ブザー | `pot-110-22` | `鳴動状態` / `鳴動周波数` |

### 2. 蓋の開閉

| 概念クラス | ID | 主な属性 |
|---|---|---|
| 蓋 | `pot-100-11` | `開閉` |
| 蓋センサ : **センサ** | `pot-110-15` | `検出値` / `on継続時間` |

### 3. 水位の判定と表示

| 概念クラス | ID | 主な属性 |
|---|---|---|
| 満水センサ : **センサ** | `pot-110-11` | `検出値` / `デフォルト値 = on` (`pot-110-12`) |
| 第n水位センサ : **センサ** | `pot-110-13` | `番号` / `検出値` / `デフォルト値 = off` (`pot-110-14`) |
| 給水線 | `pot-110-19` | `位置 = 満水センサより若干下` / `水面` |
| 水位判定 | `pot-280` | `水量区分` (適正 / 満水 / 空) / `on検出センサ数` / `判定時点` |
| 水位メータ | `pot-120-11` | `セル数` / `表示パターン` |
| インジケータセル : **ランプ** | `pot-120-11` | `番号` / `対応センサ` / `点灯状態` |

### 4. 温度制御

| 概念クラス | ID | 主な属性 |
|---|---|---|
| サーミスタ : **センサ** | `pot-110-16` | `検出水温` / `測定範囲 = -10〜150°C` |
| ヒータ | `pot-110-17` | `動作状態` |
| アイドル : **ポット状態** | `pot-330` | `状態名称` / `制御方式` / `操作量M = 0%` / `制御周期` |
| 沸騰行為 : **ポット状態** | `pot-310` `pot-311` `pot-312` | `状態名称` / `段階` (加熱中 / カルキ抜き中) / `目標温度 = 100°C` / `制御方式 = 目標温度ON/OFF方式` / `操作量M` / `カルキ抜き経過時間` |
| 保温行為 : **ポット状態** | `pot-320` | `状態名称` / `段階` (保温中 / 給湯中) / `保温モード` / `目標温度 Tg` / `制御方式 = PID方式` / `T0,T1,T2` / `M1` / `操作量 M0` |
| 保温モード設定 | `pot-240` | `保温モード` (高温 / 節約 / ミルク) / `目標温度` (98 / 90 / 60°C) / `適用状態` |
| 沸騰ボタン : **ボタン** | `pot-230` | `押下状態` / `有効性` |
| 保温設定ボタン : **ボタン** | `pot-240` | `押下状態` / `有効性` |
| 沸騰ランプ / 保温ランプ : **ランプ** | `pot-310-11` `pot-320-11` `pot-330-41` | `点灯状態` |
| 温度/モード表示窓 : **表示窓** | `pot-310-12` `pot-320-12` `pot-330-42` `pot-240-41` | `温度表示` / `モード表示` |

### 5. ロックと給湯

| 概念クラス | ID | 主な属性 |
|---|---|---|
| 給湯口 | `pot-100-13` | `ロック` |
| ポンプ | `pot-110-20` | `動作状態` |
| ロック状態 | `pot-250` | `ロック状態` |
| 給湯状態 | `pot-260` | `給湯中` / `給湯不可の理由` |
| 解除ボタン : **ボタン** | `pot-250` | `押下状態` / `有効性` |
| 給湯ボタン : **ボタン** | `pot-260` | `押下状態` / `給湯可否` |
| ロックランプ : **ランプ** | `pot-250-21` | `点灯状態` |

### 6. エラーの検出と処置

| 概念クラス | ID | 主な属性 |
|---|---|---|
| ヒータ用電源 | `pot-110-18` | `供給状態` |
| エラー検出 | `pot-500` | `エラー状態` / `エラー種別` / `検出周期` / `前回検出水温` / `98°C超過継続時間` / 各しきい値 |
| エラー : **ポット状態** | `pot-500` / 状態遷移シート6.1・6.2 | `状態名称` / `エラー種別` / `検知条件` / `想定原因` / `実施した処置` / `ブザー残り鳴動時間` / `状態の性質 = トラップ状態` / `解除方法 = コンセント抜き差し` |

**エラー と エラー検出 を分けている理由**: エラー検出は常駐して監視値を持ち続ける
(1分周期の水温検出, 98°C超過の継続時間). エラーは検知後に生成され, 温度制御行為の位置に入る
状態そのものを表す.

### 共通クラス

役割名 (オブジェクト名) とクラス名を分ける. 現行版は `沸騰ボタン : 沸騰ボタン` のように
同じ語を繰り返していたが, 分析語彙が無駄に膨らむのでやめた.

| クラス | 実体 | 根拠 |
|---|---|---|
| **ボタン** | 沸騰 / 保温設定 / 解除 / 給湯 | 4つとも 100msec 以上の押下で判定 (`pot-230-11`, `pot-240-21`, `pot-250-21`) |
| **ランプ** | 沸騰 / 保温 / ロック / インジケータセル×4 | 点灯・消灯しかない. `image13.png` が「ランプ」と明記. `pot-120-11` は「インジケータセルと呼ばれるランプ」 |
| **センサ** | 満水 / 第n水位×4 / 蓋 / サーミスタ | sheet3 ＜＜内部構成＞＞ の検出部品 |
| **表示窓** | 温度/モード表示窓 | ハードウェアは1つの窓 (`pot-240-41`「温度/モード表示窓に」). **温度表示とモード表示に分割しない** |
| **ポット状態** | アイドル / 沸騰行為 / 保温行為 / エラー | 状態遷移シート 6.1・6.2 が4つを並列のトップレベル状態として描く |

`ポット状態` を立てたのは重要で, これがないと図04のエラーを「温度制御行為」と呼ぶ必要が出て
しまう. エラーは温度制御行為ではない.

### 操作パネル部品について

ボタン・ランプ・表示窓は sheet3 の操作パネル表 (行93〜104) と `image13.png` にのみ現れ,
**`pot-100-*` の個別IDを持たない**. 振る舞いは sheet4 の `pot-230` / `pot-240` / `pot-250` /
`pot-260` / `pot-280` / `pot-310` / `pot-320` / `pot-330` 側で規定されるため, 図では
それぞれの機能 package に配置している.

### モデル化にあたっての判断

- **満水センサは第n水位センサ配列の n+1 番目としてモデル化しない.**
  `pot-280` の説明に「満水を判断するセンサーだけは他のセンサーでは代用できないので, 別な機能と
  位置づける」とあるため, 別の概念クラスとする.
- **ヒータ と ヒータ用電源 は別オブジェクト.** `pot-110-18` の説明に「電力供給状態≠ヒータ
  動作中. ヒータon=ヒータ動作中」とある. `pot-500-*` が off にするのは**ヒータ用電源**であって
  ヒータの操作量ではない. 図04はこの分離が効いている例で, ヒータが止められない故障でも
  電源を切れば加熱は物理的に止まる.
- **温度制御行為は「現在存在する行為オブジェクト」で表す.** 図01はアイドル, 図02は沸騰行為,
  図03は保温行為, 図04はエラーのオブジェクトが存在する. シーンによってオブジェクトの個数と
  種類が変わることで, オブジェクト図としての意味が出る.
- **インジケータセルは4個の実体を描くが, クラス図での多重度は `1..*` とする.**
  `pot-120-11`「水位センサと同数」, `pot-280` 説明「コストダウンによって数が変動する可能性」.
- **モデル化の対象外**
  - キッチンタイマ (タイマボタン, タイマ残り時間表示窓) … sheet3 の操作パネル表にのみ存在し,
    sheet4 に対応する `pot-NNN` 要求が存在しない
  - 取手 `pot-100-14`, 水路 `pot-110-21` … 制御に関与しない受動部品
  - ブザーの鳴動元のうち `pot-230-11` (沸騰ボタン 100msec) と `pot-240-21` (保温設定ボタン
    100msec) は**未反映**. 「要決着」の項を参照

## 「満水」の解釈

要求仕様において**満水センサ on は「水量が適正」ではなく「異常」**である.

- `pot-110-11` 満水センサ on = 水位が**許容上限を超えている**
- `pot-280-11` 蓋センサ on から 3sec 経過時に満水センサ on → 許容上限超えと判断 = 水量異常
- `pot-220-31` 水量が異常な場合, 蓋が閉じられても状態は**アイドルのまま** (沸騰しない)
- `pot-110-19` 給水線は満水センサの位置より**若干下**

したがって「水を入れて蓋を閉めたら沸騰が始まる」状態は**給水線まで**入れた状態
(満水センサ off, 第1〜第4水位センサすべて on, 水位メータ 4 セル全点灯) である.
図02はこの解釈で描いている.

## 決着済みの判断

### 1. 満水センサ on を温度制御停止条件に含める

仕様書内で記述が食い違っていた.

| 出典 | 温度制御停止の条件 |
|---|---|
| 状態遷移シート 7章 イベント表 (`images/image15.png`) | 蓋センサ off, 全水位センサ off, **満水センサ on** |
| sheet3 `B149` | 「第n位センサーON」かつ「満水センサがOFF」の場合のみ沸騰ボタン・ヒーターが動作 |
| sheet4 `pot-310-31` / `pot-320-31` / `pot-330-12` / `pot-330-13` | エラー検知, 蓋センサ off, 全水位センサ off の**3つのみ** |

**決定: 満水センサ on を温度制御停止条件に含める** (状態遷移シートと sheet3 に従う).
満水センサは異常を示す要素としての性格が強く, `pot-110-12` がデフォルトを on (故障時は
安全側) と定めているのと整合するため.

補足として, 実際の運用では**この条件が単独で効く場面はほとんどない**. 物理的に水を足すには
蓋を開ける必要があり, 蓋センサが 1sec 以上 off になった時点で `pot-221-11` により温度制御行為が
先に中止されるからである. その後蓋を閉じれば `pot-220-11`→`pot-280-11` の経路で満水が判定され,
`pot-220-31` によりアイドルのままとなる. したがってこの条件が意味を持つのは,
満水センサの故障や, 沸騰による水面の変動といったケースである.

### 2. sheet3 の安全性要求 vs `pot-500-*` → sheet4 を正とする

sheet3 の `安全性要求` (行137〜146) は 100°C / 5°C / エラー2種と書くが, sheet4 の
`pot-500-11` / `-21` / `-31` は **110°C / 8°C / エラー3種** (温度下がらずエラーを含む).
**sheet4 (`pot-500-*`) を正**とする (`CLAUDE.md` の指示と一致).

### 3. ランプの点灯条件 → `pot-310-11` / `pot-320-11` / `pot-330-41` を正とする

sheet3 `C98`「沸騰中以外点灯」に対し, `pot-330-41` はアイドル時に沸騰ランプ・保温ランプを
**ともに消灯**とする. sheet3 の `B151`「ヒーターが動作していない場合, 沸騰ランプと保温ランプは
消灯」は sheet4 と一致するため, sheet4 側を採用する.

### 4. 未定義の数値は設計モデリング時に決める

以下は分析モデルでは**【仕様未定義】のまま扱う**. 設計モデリングの段階で決めればよい.

- 制御周期の値 (`pot-400-11`, `pot-400-31`)
- PID 係数 Kp / Ki / Kd の値 (`pot-400-31`, 「水の温度特性により決定される」とのみ)
- ブザーの鳴動周波数 (`pot-110-22`, `○○KHz` のまま)

### 5. 欠番はキッチンタイマの要求群

`pot-270` および `pot-330-31` の欠番は, 元はキッチンタイマの要求群だった箇所である.
今回のモデリングではキッチンタイマを対象外としているため, 欠落したままでよい.

### 6. package はハードウェア分類ではなく機能で切る

当初は「外観 / ハードウェア境界 / 操作パネル部品 / 制御」という区分だったが, 前3つが物理分類で
最後だけが機能分類という**軸の混在**になっていた. sheet4 自身が機能で構成されていることを
根拠に, 6つの機能 package に切り直した. 詳細は「図の読み方」を参照.

### 7. 関連は3種に分け, 説明線は各図4本まで

オブジェクト図の線を 合成 / リンク / (線を引かない) の3種に分け, 判定手順を定めた.
非標準の「説明線」は各図4本までとし, ラベルを `<<説明線:用途>>` で始めて逸脱を明示する.
詳細は「関連の判定手順」を参照.

これに伴い次を改めた.

- 破線 `..>` を「参照/指示/条件」に多用していたのをやめた (各図10本以上 → 4本)
- `蓋 → 蓋センサ`, `貯水部 → サーミスタ / ヒータ` を合成からリンクに変更
- `ポット → 温度制御行為` を `ポット — 現在状態 : ポット状態` に変更
- 複合条件を note + 制約 `{…}` に移した
- 同型4実体を畳み込み表記にした
- 共通クラス (ボタン / ランプ / センサ / 表示窓 / ポット状態) を導入した

## 要決着の項目

### A. `pot-500-21` が自然冷却中に成立してしまう

`pot-500-21` の文言は「保温の各モードになって**3分以上**水温が **98°C を超えていた**場合,
ヒータ用電源を off して 30秒間ブザーを鳴らす」である. しかし:

- 沸騰行為は 100°C まで加熱してカルキ抜きを 3分行う (`pot-311-11`)
- その後 `pot-312-11` で保温行為に移る
- sheet3 `B152` は「保温モードに設定した際に 100°C でなかった場合, 一度沸騰させたあと,
  **自然に冷ましながら**設定温度に保つ」とする
- 貯水部は「断熱性の高いステンレス素材」(`pot-100-15`) なので, 100°C から 98°C を割るまでに
  3分以上かかることは十分ありうる. 特に節約モード (90°C) やミルクモード (60°C) では
  自然冷却の時間が長い

つまり**正常な冷却中に温度下がらずエラーが誤検知される**. 解釈の候補:

1. `pot-500-31` と同じく「**ヒータ制御中**に」の限定を補う (操作量 M > 0 のときだけ判定する)
2. 3分の計測開始を「保温の各モードになって」ではなく「**目標温度に到達してから**」とする
3. 判定温度 98°C をモードごとの目標温度に連動させる

### B. エラー時の表示が規定されていない

`pot-500-*` はヒータ用電源の遮断とブザー鳴動しか規定しておらず, **表示について何も言わない**.
図04では以下が【仕様未定義】になった.

| 出力 | 図04で置いた推定値 | 推定の根拠 |
|---|---|---|
| 沸騰ランプ / 保温ランプ | ともに消灯 | sheet3 `B151`「ヒーターが動作していない場合, 沸騰ランプと保温ランプは消灯」 |
| 温度/モード表示窓 (温度) | 表示を継続 (99) | `pot-330-42` はアイドル時のみ非表示を規定. エラー時は未規定 |
| 水位メータ / インジケータセル | 通常表示のまま | `pot-221-12` は蓋開時の全消灯のみ規定 |
| 保温設定ボタン / 解除ボタン の有効性 | 未定 | `pot-240-21`・`pot-250-31` はエラー時に言及しない. 一方 状態遷移シート 6.2 はエラーを全イベント無視のトラップ状態とする |

### C. `pot-500-31` の 8°C の境界

`pot-500-31`「目標温度よりも水温が **8°C下がり**」は境界を書いていない. ミルクモード (60°C) の
とき, 52°C ちょうどが該当するのかしないのかが決まらない. 同じ `pot-500` 群でも `pot-500-11` は
「110°C を**超えた**場合」と明示しており, 書き分けが不統一である. 図では
「目標温度より 8°C 低い (60→52°C)」と書き, 境界の扱いは【仕様未定義】と注記している.

### D. ブザーの鳴動元

sheet4 に明記されている鳴動元は次の3つ.

| 主体 | 鳴動 | ID |
|---|---|---|
| 沸騰ボタンの受付 | 100msec × 1 | `pot-230-11` (`pot-230-21` は「ブザーは鳴らさず」と明示) |
| 保温設定ボタンの受付 | 100msec × 1 | `pot-240-21` |
| エラー検出 | 30秒 | `pot-500-11` / `-21` / `-31` |

**現在の図はエラーからの1本しか引いていない** (`pot-230-11` / `pot-240-21` は未反映).
またブザーは 100msec / 30秒 と鳴り方が異なるので, 水位メータの `表示パターン` と同様に
`鳴動パターン` 属性を持たせるのが素直である.

さらに sheet3 にしかない鳴動が2件ある.

| 出典 | 内容 | sheet4 の状況 |
|---|---|---|
| sheet3 `B153` | ボタンが押されたらブザーを1回鳴動 | 解除ボタン `pot-250-21`・給湯ボタン `pot-260-11` には鳴動の記述が一切ない |
| sheet3 `B155` | **沸騰状態終了時, ブザーを3回鳴動** | `pot-312-11` に記述なし. **お湯が沸いたことを知らせる手段が sheet4 だけでは存在しない** |

### E. その他の未規定

- アイドル時に保温モードの ▼ を表示するかどうか (`pot-240-41` は表示位置だけを規定)
- コンセント挿入直後のインジケータセルの初期状態 (`pot-221-12` は蓋開時の全消灯のみ規定)
- 水量異常の「判断」は蓋センサ on から 3sec 経過時の一度だけ (`pot-280-11`/`-12`) だが,
  保温中に給湯で水位が下がった場合の再判定については `pot-320-31` の「全水位センサ off」
  しか規定がない

### F. 追加すべきシーンの候補

現在の4枚では以下の要求が主役として確認できない. 優先順に:

| 追加シーン | 確認できるようになる要求 |
|---|---|
| 蓋開による温度制御中止 | `pot-221-11` (1sec off), `pot-221-12` (インジケータセル全消灯), `pot-221-13` (ロック強制解除) |
| 満水異常 | `pot-280-11` (許容上限超え), `pot-280-31` (満水点滅 500ms点灯/200ms消灯), `pot-220-31` |
| 給湯中 | `pot-260-11` (ポンプ作動), `pot-260-21` (途中停止), `pot-230-21` (沸騰ボタン無効), `pot-250-31` (解除ボタン無効) |
| カルキ抜き中 | `pot-311-11` (100°C到達後 3分, ヒータ on 継続), `pot-312-11` |

## レビュー履歴

- **2026-08-19** Codex (`gpt-5.2-codex`) による構成レビューを実施. 指摘のうち以下を反映した.
  - package をハードウェア分類から機能分類へ変更 (決着済みの判断 6)
  - `pot-260-21` (給湯中の停止) を給湯中でないシーンで引用していた誤りを修正 (図04)
  - 水量適正の判定根拠を `pot-280-21` (表示要求) から `pot-280-11`/`-12` の不成立へ修正 (図02)
  - `前回検出水温` の数値に【シーン仮定】を併記 (図03, 図04)
  - 図04の水位メータと個別セルで根拠が食い違っていたのを【仕様未定義】に統一
  - 満水センサ off の根拠に【シーン仮定】を併記 (図02)
  - `pot-500-31` の 8°C の境界問題を「要決着 C」として記録
  - 追加シーンの候補を「要決着 F」として記録
  - 貯水部・操作パネルに `取り付け部品` / `配置部品` 属性を追加し, 関連線の省略を補った
- 併せて, 図01の沸騰ボタンの根拠が `pot-220-31` (ボタンについて何も述べていない) になっていた
  のを `pot-230-11` (保温行為中でない) に修正した.
- **反映しなかった指摘**: 「図02以降を部分図にする」。4枚を全量スナップショットで揃えている
  からこそ上の「4枚が受け持つ値の範囲」の比較が成立する. 部分図にすると, ある属性が
  描かれていないとき「変化なし」なのか「関心外」なのか読者に判別できない.
- **2026-08-20** 線種の意味を検討し, 破線 `..>` が UML の依存の流用だったことを確認.
  関連を3種に分ける規約を定めて4枚を再生成した (決着済みの判断 7). Codex に再度レビューを依頼し,
  以下を反映した.
  - `蓋 → 蓋センサ` の合成は根拠不足 (`pot-110-15` はセンサの取り付け位置を書いていない) → リンクへ
  - `貯水部 → サーミスタ / ヒータ` も同様 → 作用対象としてのリンクへ
  - `ポット → 温度制御行為` は図04のエラーを含められない → `現在状態 : ポット状態` へ
  - 説明線は4本に絞り, ラベル接頭辞 `<<説明線:用途>>` で非標準であることを明示
  - 畳み込み表記の対応は同一線4本ではなくリンク1本 + 制約 `{1対1×4}`
  - 共通クラスは `ボタン` / `ランプ` / `センサ` / `表示窓` を採用. `温度表示`/`モード表示` の
    オブジェクト分割は不採用 (ハードウェアは1つの窓)
  - 畳み込み表記と共通クラスの型付けは `docs/analysis-analog/` の図から取り入れた
  - 図の寸法は 5101〜5515px 幅 → 4916〜5269px 幅に縮小. 幅の主因は破線ではなく **note の行の長さ**
    だったので折り返しで対処した (高さの主因は legend)
- **2026-08-20 (2回目)** 再生成した4枚を Codex に再レビューさせ, 以下を修正した.
  - 図02・図03の状態 note で `pot-230-11` の条件から「**給湯中でない**」が抜けていた
    (原文は「保温行為中で給湯中でなければ」). テンプレート置換時の脱落
  - 状態 note の制約本体に **満水センサ on** が入っておらず, 「決着済みの判断 1」および
    図01の補足文と食い違っていた. 4枚の制約に `∨ 満水センサon (決着事項)` を追加
  - 図04の `保温モード設定.適用状態 = 停止 (エラーのため)【pot-500-21】` は根拠が強すぎた.
    `pot-500-21` が規定するのはヒータ用電源 off とブザー鳴動のみでモード設定値は保持されるため,
    `未適用 (保温行為が存在しないため)` に改めた
  - ブザーだけ標準の線を1本も持たない状態になっていたので, 判定表に「構造線を引かない」理由を明記
  - UML 用語を精密化: 「オブジェクト図で実体関係として通常表す線はリンク (関連のインスタンス)」,
    「合成もリンクであり, 合成自体はクラス図の関連端の性質」, 「`{…}` は自然言語の制約であって
    OCL 等の形式制約ではない」
  - Q1 の判定を「Aを捨てるとき一緒に捨てるか」から「仕様が部位・構成として明示し, この図で
    全体-部分として読ませる必要があるか」に狭めた. 前者を文字通り適用すると sheet3 ＜＜内部構成＞＞
    の9部品がすべて `ポット` からの合成になり判定表と矛盾するため
  - **反映しなかった指摘**: 「図01〜03の `エラー検出 ..> ヒータ用電源` / `..> ブザー` は
    非エラーシーンでは潜在仕様であって冗長」。4枚で線の構成を揃えることが比較可能性の前提なので残した

## レンダリング

```bash
# 文法検証 (フォント不要)
plantuml -charset UTF-8 -checkonly docs/analysis/*.puml

# PNG 生成
plantuml -charset UTF-8 -DPLANTUML_LIMIT_SIZE=32768 -tpng docs/analysis/*.puml
```

`-DPLANTUML_LIMIT_SIZE` は必須. 既定値 4096px では図が右端で切れる.
出力ファイル名は `.puml` のファイル名ではなく **`@startuml` の後ろの名前**で決まるので,
両者を一致させておくこと.

必要な環境:

- `plantuml` (1.2026.2 で確認), `java`, `graphviz` (`dot`)
- 日本語フォント `Noto Sans CJK JP` (各 `.puml` の `skinparam defaultFontName` で指定)
